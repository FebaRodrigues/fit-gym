{
  "name": "fitness-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "fitness-tracker/Server"
  ],
  "scripts": {
    "build": "cd fitness-tracker/Server && npm install && npm run build",
    "start": "cd fitness-tracker/Server && npm start",
    "dev": "cd fitness-tracker/Server && npm run dev"
  },
  "engines": {
    "node": ">=18.x"
  }
}
